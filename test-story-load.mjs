import { terminalVelocityContent } from './src/stories/terminal-velocity/index.ts';

console.log('Story ID:', terminalVelocityContent.story.id);
console.log('Story Name:', terminalVelocityContent.story.name);
console.log('Evidence count:', terminalVelocityContent.evidence.length);
console.log('Characters count:', terminalVelocityContent.characters.length);
console.log('✓ Story content loads successfully!');
